on:
  push:
    branches:
      - '*'
    tags:
      - '*'
  pull_request:
    branches:
      - master
jobs:
  build:
    runs-on: windows-latest
    steps:
      - run: |
          $PSVersionTable
      - run: |
          cmd.exe /c "echo hello 1>&2"
          cmd.exe /c "echo world 1>&2"
          cmd.exe /c "echo asdf 1>&2"
          cmd.exe /c "echo aaaa 1>&2"
          cmd.exe /c "echo bbbb 1>&2"
#      - run: |
 #         printenv|sort
  #    - run: |
   #       echo '${{ github.event_name == 'push' && 'it is a push' || 'it is not a push' }}'
#      - run: |
 #         printenv|sort
  #    - name: Dump GitHub context
   #     env:
    #      GITHUB_CONTEXT: ${{ toJson(github) }}
     #   run: echo "$GITHUB_CONTEXT"
      #- uses: actions/checkout@v2
       # with:
        #  fetch-depth: 0 # fetch all history
#      - run: |
 #         git diff ${{ github.event.before }}..${{ github.event.after }}


#      - uses: actions/checkout@bbe3883b650b0759165342a3afcf5365aebae8a6
 #     - uses: actions/checkout@bbe3883b650b0759165342a3afcf5365aebae8a6
  #      with:
   #       fetch-depth: 0
    #  - uses: actions/checkout@bbe3883b650b0759165342a3afcf5365aebae8a6
     #   with:
      #    fetch-depth: -1

#      - uses: actions/checkout@bd912f9899e75f00fcf4fffdd0068a34ce923113
 #       with:
  #        fetch-depth: 0
#      - run: |
 #         git config user.name github-actions
  #        git config user.email github-actions-bot@users.noreply.github.com
   #       git checkout -b testing-temp
    #      git push origin --set-upstream testing-temp
     #     git branch -d testing
      #    git push -f origin :testing
       #   git checkout -b testing/foo
        #  git push origin --set-upstream testing/foo
#      - uses: actions/checkout@bd912f9899e75f00fcf4fffdd0068a34ce923113
 #     - uses: actions/checkout@bd912f9899e75f00fcf4fffdd0068a34ce923113
  #      with:
   #       ref: testing/foo

#  job1:
 #   runs-on: ubuntu-latest
  #  steps:
   #   - run: |
    #      printenv|sort
     # - uses: actions/checkout@bd912f9899e75f00fcf4fffdd0068a34ce923113
      #  with:
       #   fetch-depth: 0
#      - shell: 'node {0}'
 #       run: |
  #        var minutes = 1
   #       setTimeout(function(){ console.log('done') }, minutes * 60 * 1000)
#  job2:
 #   needs: job1
  #  runs-on: ubuntu-latest
   # steps:
    #  - uses: actions/checkout@bd912f9899e75f00fcf4fffdd0068a34ce923113
     #   with:
      #    fetch-depth: 0









#      - shell: 'node {0}'
 #       env:
  #        GITHUB_TOKEN: ${{ github.token }}
   #     run: |
    #      console.log(process.env.GITHUB_TOKEN.substr(0, 10))
     #     console.log(process.env.GITHUB_TOKEN.substr(10))
      #- shell: 'node {0}'
       # run: |
        #  var minutes = 10
         # setTimeout(function(){ console.log('done') }, minutes * 60 * 1000)

#      - run: |
 #         printenv|sort
  #    - name: Dump GitHub context
   #     env:
    #      GITHUB_CONTEXT: ${{ toJson(github) }}
     #   run: echo "$GITHUB_CONTEXT"
      #- uses: actions/checkout@v2
#      - run: |
 #         npm install @actions/github .
  #    - shell: node {0}
   #     run: |
    #      var gh = require('@actions/github')
#      - run: |
 #         set -x
  #        rm -rf "$RUNNER_TOOL_CACHE" || :
   #       mkdir -p "$RUNNER_TOOL_CACHE"
    #  - uses: actions/setup-node@users/ericsciple/m262ghes_403
     #   with:
      #    node-version: 12
#      - run: |
 #         printenv|sort
#      - shell: node "{0}"
 #       run: |
  #        console.log('hello')
   #       console.log('world')
