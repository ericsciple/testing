name: CI

on: [push]

jobs:
  build:
    runs-on: { pool: default }
    
    steps:
      - uses: actions/checkout@v1
      - uses: actions/checkout@v1
      - run: pwd ; ls -la
      - run: |
          echo hash = ${{ hashFiles('**/+(README).md') }}

