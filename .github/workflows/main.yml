name: CI

on: [push]


jobs:
  build:
    #runs-on: self-hosted
    runs-on: [self-hosted, macos]
    #runs-on: [self-hosted, linux]
    #runs-on: windows-latest
    #runs-on: ubuntu-latest
      
      
#    container: alpine/git
 #   services:
  #    nginx:
   #     image: nginx
    #    ports:
     #     - 8080:80
      #  env:
       #   NGINX_PORT: 80

    #steps:
     # - run: printenv|sort
      #- uses: ericsciple/checkout@users/ericsciple/action

    steps:
      - uses: actions/checkout@v1
      - run: |
          echo "::add-matcher::$PWD/csc.json"
          echo "SnippetParsing.cs(7,2): error CS1022: Type or namespace definition, or end-of-file expected [$HOME/repos/actions/runner/_layout/_work/ericsciple/testing/BumblebeeTests/BumblebeeTests.csproj]"




#- uses: actions/checkout@releases/v1
      #- run: git config --list
      #- uses: ericsciple/checkout@users/ericsciple/action
#      - run: |
 #         echo ::add-mask::hello%0Aworld
  #        echo hello
   #       echo world
#      - uses: ericsciple/checkout@users/ericsciple/action
 #       with:
  #        ref: foo30-lightweight
   #   - run: pwd ; ls -la
    #  - uses: ericsciple/checkout@users/ericsciple/action
     #   with:
      #    ref: foo30-annotated
       #   path: other
#      - run: pwd ; ls -la
 #     - run: cd ../testing ; ls -la
  #    - uses: ericsciple/checkout@users/ericsciple/action
   #     with:
    #      repository: ericsciple/checkout
     # - run: cd ../checkout ; ls -la




#      - uses: actions/checkout@v1
#      - run: dir
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles('**/READM?.md') }}
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles('**\\READM?.md') }}
   #   - name: asdf
    #    run: |
     #     echo hash = ${{ hashFiles('**\READM?.md') }}
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles('C:\\runner\\_layout\\_work\\testing\\testing\\**\\READM?.md') }}
      #- run: pwd ; ls -la
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles('C:\\runner\\_layout\\_work\\testing\\testing\\**\\READM?.md') }}
   #   - name: asdf
    #    run: |
     #     echo hash = ${{ hashFiles('C:\\runner\\_layout\\_work\\testing\\testing\\**/READM?.md') }}
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles('**/READM?.md') }}
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles(format('{0}/{1}', github.workspace, 'READM?.md')) }}
      #- name: asdf
      #  run: |
      #    echo hash = ${{ hashFiles('nosuch/READM?.md') }}
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles('/nosuch/READM?.md') }}
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles('**/READM?.md') }}
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles('**/readm?.md') }}
#      - name: asdf
 #       run: |
  #        echo hash = ${{ hashFiles('**/+(README).md') }}

