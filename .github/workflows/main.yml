on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - id: sets-a-secret
        run: |
          echo "::set-output name=my-secret::$((RANDOM))"
      - name: My script only contains a bash comment
        run: |
          # ${{ steps.sets-a-secret.outputs.my-secret }}
