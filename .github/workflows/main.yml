name: CI

on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ericsciple/setup-runner@master
        with:
          token: ${{ secrets.RUNNER_CONFIG_TOKEN }}
          ref: master
