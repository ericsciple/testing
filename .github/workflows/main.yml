name: CI

on: [push]

jobs:
  build:
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        runs-on: [ubuntu-latest, windows-latest, macOS-latest]

    runs-on: ${{ matrix.runs-on }}
    
    steps:
    - run: exit 1
