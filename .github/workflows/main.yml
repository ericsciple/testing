on: push
jobs:
  build:
    strategy:
      matrix:
        include:
          - config: debug
            arch: x86
          - config: debug
            arch: x64
    runs-on: [ubuntu-latest]
#    container:
 #     image: ubuntu
    steps:
      - run: |
          echo hi
#      - run: |
 #         echo ::group::foo
  #        echo hello
   #       echo hello 2
    #      echo ::group::nested
     #     echo inside nested
      #    echo ::error::this is an error
       #   echo after error
        #  echo ::endgroup::
         # echo asdf
#      - if: "${{ !startsWith(github.ref, 'refs/tags/') }}"
 #       run: pwd
  #    - run: ps -ef
   #   - run: cat /home/runner/runners/2.165.2/.runner
    #  - run: |
     #     echo "$GITHUB_CONTEXT"
      #  env:
       #   GITHUB_CONTEXT: ${{ toJson(github) }}
#      - run: |
 #         cat cat /home/runner/runners/2.165.2/_diag/W*




      #- uses: actions/checkout@v2
      #- run: echo "hashFiles=${{ hashFiles('README.md') }}"
#   - shell: cmd
 #    run: |
  #     set
#   - run: |
 #      printenv|sort
  # - name: Dump GitHub context
   #  env:
    #   GITHUB_CONTEXT: ${{ toJson(github) }}
#     run: echo "$GITHUB_CONTEXT"
 #  - run: |
  #     ls -la /home/runner/runners/2.165.2/
   #- run: |
    #   ls -la /home/runner/runners/2.165.2/_diag
#   - run: |
 #      cat /home/runner/runners/2.165.2/_diag/W*
  # - run: |
   #    ps -ef
#   - uses: actions/setup-node@v1
 #    with:
  #    node-version: 12.x
   #- uses: actions/setup-node@v1
    # with:
     # node-version: 12.x






#  push:
 #   branches:
  #    - master
#  issues:
 # issue_comment:
  #pull_request_review_comment:
#jobs:
 # build:
#    if: ${{ true }}
  #  runs-on: [ubuntu-latest]
    #runs-on: self-hosted
    #container: alpine:latest
   # steps:
    #  - uses: ericsciple/checkout@testing-download
     # - run: exit 1
#      - name: Dump GitHub context
 #       env:
  #        GITHUB_CONTEXT: ${{ toJson(github) }}
   #     run: echo "$GITHUB_CONTEXT"
    #  - if: contains(github.event.issue.labels.*.name, 'security')
     #   run: echo "${{ join(github.event.issue.labels.*.name) }}"
#      - env:
 #         true_lower: true
  #        true_pascal: True
   #       true_upper: TRUE
    #    run: |
     #     printenv | sort | grep true_
#      - id: myid
 #       if: ${{ true }}
  #      name: the display name
   #     run: |
    #      echo "action id is ${{ github.action }}"
     #     echo "action id is $GITHUB_ACTION"
#      - uses: actions/checkout@v2
 #     - uses: ./docker-action
  #    - run: |
   #       echo '{ "problemMatcher": [ { "owner": "asdf", "pattern": [ { "regexp": "^(.+):\\sline\\s(\\d+),\\scol\\s(\\d+),\\s(Error|Warning|Info)\\s-\\s(.+)\\s\\((.+)\\)$", "file": 1, "line": 2, "column": 3, "severity": 4, "message": 5, "code": 6 } ] } ] }' > "asdf.json"
    #      echo "::add-matcher::$PWD/asdf.json"
     #     echo "::remove-matcher::$PWD/asdf.json"

