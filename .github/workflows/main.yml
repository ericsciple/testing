name: "CI"

on:
  push:
  pull_request:
    branches: [ submodule-testing ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "id=${{ github.event.repository.owner.id }}"
      - uses: actions/checkout@users/ericsciple/21-10-org
        with:
          submodules: true
