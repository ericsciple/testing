name: "CI"

on:
  push:
  pull_request:
    branches: [ submodule-testing-2 ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "id=${{ github.event.repository.owner.id }}"
      - uses: actions/checkout@users/ericsciple/21-10-org
        with:
          submodules: recursive
      - run: |
          ls -la testing/
      - run: |
          ls -la testing/runner/
      - run: |
          ls -la testing/toolkit/
      - uses: actions/checkout@users/ericsciple/21-10-org
        with:
          submodules: recursive
      - run: |
          ls -la testing/
      - run: |
          ls -la testing/runner/
      - run: |
          ls -la testing/toolkit/
