on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          printenv|sort
#      - shell: node "{0}"
 #       run: |
  #        console.log('hello')
   #       console.log('world')
