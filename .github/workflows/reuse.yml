name: zzz_ericsciple_reuse
on:
  workflow_call:
#    outputs:
 #     foo:
  #      description: foo
   #     value: ${{ 'bar' }}
defaults:
  run:
    shell: sh
jobs:
  hello:
    runs-on: ubuntu-latest
    steps_:
      - run: echo hi
